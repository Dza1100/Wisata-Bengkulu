const fs = require('fs'); const content = fs.readFileSync('public/bengkulu_map.svg', 'utf8'); const paths = [...content.matchAll(/<path[^>]+d=\x22([^\x22]+)\x22[^>]*>/gi)]; const lengths = paths.map(p => ({ dLength: p[1].length, full: p[0] })).sort((a,b) => b.dLength - a.dLength); console.log('Total paths:', paths.length); console.log('Top 20 path lengths:', lengths.slice(0, 20).map(x => x.dLength));